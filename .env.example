# Optional — only needed if you hit rate limits on GoPlus's free tier
GOPLUS_APP_KEY=
GOPLUS_APP_SECRET=

# Required for AI explanations (falls back to a templated explanation if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
