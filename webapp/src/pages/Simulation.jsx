import Card from '../components/Card';

// Placeholder — scaffolded, not yet built. Phase 1 covers Home only;
// this page is next in the build queue.
export default function Simulation() {
  return (
    <Card>
      <h2 style={{ margin: 0 }}>Simulation</h2>
      <p style={{ color: 'var(--text-muted)' }}>This module is scaffolded but not yet built.</p>
    </Card>
  );
}
